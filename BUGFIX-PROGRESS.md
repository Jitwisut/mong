# สถานะการแก้บัค KORN & COINS

อัปเดตล่าสุด: 6 กันยายน 2026 (รอบที่ 2) · branch `redesign/luxury-dark-gold`

ที่มา: ตรวจทั้งเว็บ (อ่านโค้ด 3,491 บรรทัด + วัด responsive 9 ขนาดจอ) พบบัค 28 ข้อ
เอกสารนี้บอกว่าแก้ไปถึงไหนแล้ว และเหลืออะไรต้องทำต่อ

---

## ✅ Phase 1 — บัคที่ทำให้เว็บพัง / ข้อมูลหาย (เสร็จ · commit `d2998e5`)

| # | บัค | สิ่งที่แก้ | ยืนยันแล้ว |
|---|---|---|---|
| 1 | ค่าเสียใน `localStorage` ทำให้ทุกหน้าขึ้น "Application error" ถาวร | `components/wishlist.tsx` — ตรวจ shape ของข้อมูล ไม่ใช่แค่ดัก `JSON.parse` + ห่อ `localStorage` ด้วย try/catch ทั้งอ่านและเขียน | ทดสอบ 7 เคสข้อมูลเสีย เปิดได้ปกติหมด และเคสผสมยังเก็บรายการที่ดีไว้ ทิ้งเฉพาะตัวเสีย |
| 17 | effect เขียนอาเรย์ว่างทับข้อมูลที่เก็บไว้ | ใช้ state `hydrated` (ไม่ใช่ ref — ref จะเป็น true ตั้งแต่ effect รอบแรกจึงยังทับอยู่ดี) | รายการที่เก็บไว้รอดหลังรีโหลด |
| 6 | `localStorage` ไม่มี try/catch พังในโหมดส่วนตัว | ครอบทุกจุดทั้ง wishlist และ dashboard-actions | — |
| 2 | แอดมินบันทึกสินค้าสำเร็จ แต่ UI ขึ้น "เชื่อมต่อเซิร์ฟเวอร์ไม่สำเร็จ" | `admin-dashboard.tsx` — `event.currentTarget` เป็น null หลัง `await` เก็บ `const form` ไว้ก่อน | พิสูจน์ด้วยหน้าทดสอบ: `after-await=null · reset() THREW` |
| 3, 4 | ฟอร์มติดต่อ + ปุ่มสอบถามสินค้า ขึ้น "ส่งเรียบร้อยแล้ว" ทั้งที่ไม่ส่งอะไรเลย | ต่อ API จริง → เก็บลง PostgreSQL | ส่งจริง 2 รายการ ลงตาราง ภาษาไทยไม่เพี้ยน |
| 26 | ข้อความบอกจะติดต่อกลับทาง "อีเมล" แต่ฟอร์มไม่มีช่องอีเมล | เพิ่มช่องอีเมลในป๊อปอัปสินค้า | — |
| 16 | หน้า "การขาย" โชว์ตัวเลขปลอม 3/2/1 | ดึงข้อมูลจริงจาก `getInquiryCounts()` + ตารางข้อความล่าสุด | query ให้ total=2 new=2 appointments=1 ถูกต้อง |

**ไฟล์ใหม่:** `db/migrations/002_create_inquiries.sql` · `lib/inquiry-repository.ts` · `app/api/inquiries/route.ts`

**สำคัญ:** `scripts/db-migrate.mjs` เดิม hardcode ไฟล์ `001` ไว้ ทำให้ migration ใหม่ไม่มีวันถูกรัน — แก้ให้อ่านทุกไฟล์ `.sql` เรียงตามชื่อแล้ว

**กันข้อมูลหาย:** ถ้าไม่ได้ตั้ง `DATABASE_URL` API จะตอบ **503** และหน้าเว็บจะโชว์เบอร์ร้าน `088-788-9878` + อีเมล — **ไม่ขึ้นข้อความสำเร็จปลอมอีกแล้ว** (ทดสอบยืนยันแล้ว)

---

## ✅ Phase 2 — มือถือ / responsive (เสร็จ · commit `e725043`)

| # | บัค | สิ่งที่แก้ | ยืนยันแล้ว |
|---|---|---|---|
| 5 | มือถือค้นหา + ดูรายการโปรดไม่ได้เลย (ไอคอนถูก `display:none` ต่ำกว่า 768px) | ให้ไอคอนค้นหา + หัวใจแสดงทุกขนาดจอ | 320–1920px แสดงครบ · เปิด modal ค้นหาและแผงรายการโปรดบนมือถือได้แล้ว |
| 12 | ปุ่มเล็กเกินมาตรฐาน (hamburger 20×20 · ลิงก์เมนู 16–21px) | ไอคอน 40px มือถือ / 44px เดสก์ท็อป · ลิงก์เมนู 44px · "เปิดรายการ" + ลิงก์ footer 44px | วัดได้ 40/44 ครบ · ลิงก์เมนู 44px |
| 11 | Modal ไม่มี Esc / พื้นหลังเลื่อนได้ | เพิ่ม hook `components/use-modal-dismiss.ts` ใช้ร่วมทั้ง 3 จุด | Esc ปิดได้ · `body.overflow` เป็น hidden ตอนเปิดและคืนค่าเมื่อปิด |
| 14 | grid สินค้าข้าม breakpoint `lg` เหลือ 2 คอลัมน์กว้างช่วง 1024–1279px | `lg:grid-cols-3` | 1024px ได้ 3 คอลัมน์แล้ว (เดิม 2) |
| 23 | H1 สินค้า 64px ตั้งแต่ `md:` ทั้งที่คอลัมน์แคบ | เลื่อนไปเป็น `xl:` | 1024px = 40px (เดิม 64px) |
| 22 | `ScrollReset` บังคับเลื่อนขึ้นบนทุกเฟรมนาน 260ms ฝืนผู้ใช้ | เลื่อนครั้งเดียว + ยืนยันเฟรมถัดไป + ยกเลิกทันทีถ้าผู้ใช้เลื่อนเอง | — |
| 21 | "บริการ" กับ "ติดต่อเรา" ชี้ `/contact` เหมือนกัน และหน้านั้นไม่เคย highlight | รวมเป็น "บริการและติดต่อ" + ขยาย type `ActiveNav` | เมนูเหลือ 5 รายการ |
| 24 | `key={item.label}` ในผลค้นหาชนกันได้ | เปลี่ยนเป็น key จาก href | — |

**ผลรวม:** ทดสอบ 63 ชุด (7 หน้า × 9 ขนาดจอ) — **ไม่มี horizontal overflow เลยแม้แต่จุดเดียว** รวมถึงที่ 320px

---

## 🚧 Phase 3 — แผงแอดมิน (commit `ac3a28f` · **ยังไม่ได้ตรวจด้วยตา**)

โค้ดเขียนเสร็จแล้ว ผ่าน `tsc` และ `npm run build` แต่ **ยังไม่ได้เห็นหน้าจอจริง** เพราะต้องใส่ PIN เข้า `/admin` ซึ่งผมไม่กรอกรหัสให้

| # | บัค | สิ่งที่แก้ | สถานะ |
|---|---|---|---|
| 10 | พรีวิวรูปอัปโหลดพัง เพราะส่ง `blob:` เข้า `next/image` | `upload-area.tsx` — เพิ่ม `unoptimized={Boolean(previewUrl)}` | ⚠️ ต้องลองอัปโหลดรูปจริงในหน้า admin |
| 7 | มือถือไม่มีปุ่มออกจากระบบ | `dashboard-actions.tsx` — เอา `hidden sm:inline-flex` ออก | ⚠️ ต้องดูบนมือถือจริง |
| 8 | ปุ่ม "บันทึกร่าง" ไม่มี feedback เพราะ `description` ทับ `status` เสมอ | สลับเป็น `status ?? description` + ล้างเมื่อเปลี่ยนหมวด | ⚠️ ต้องกดปุ่มดู |
| 9 | ฟอร์มทิ้งข้อมูลเงียบๆ | ส่ง `subcategory` ต่อท้าย `eyebrow` · `listing_type` ลง `status` · `price` แปลงเป็น `฿15,000` | ⚠️ ต้องลองเพิ่มสินค้าจริง |
| 15 | ไม่มี rate limit ที่หน้าล็อกอิน (PIN 6 หลัก brute-force ได้) | `lib/rate-limit.ts` — 5 ครั้ง / 15 นาที ต่อ IP | ✅ **ทดสอบแล้ว** — ครั้งที่ 6 ตอบ 429 และ IP อื่นไม่โดนล็อกตาม |
| 28 | `MobileDashboardMenu` (sticky top-0 z-50) กับแถบ action (sticky top-0 z-40) ปักที่เดียวกัน | ให้แถบ action เป็น `static md:sticky` | ⚠️ วัดได้ว่าแถบนี้สูง **209px** ที่ 375px — ปักไว้บนมือถือกินจอ 26% อยู่แล้ว แต่ยังไม่ได้ยืนยันว่าซ้อนกันจริงตอนเลื่อน |

---

## ✅ Phase 4 — เก็บรายละเอียด (เสร็จ)

| # | บัค | สิ่งที่แก้ | ยืนยันแล้ว |
|---|---|---|---|
| 13 | `metadataBase` fallback เป็น `localhost` รูป OG ตอนแชร์จะพัง | เพิ่ม `NEXT_PUBLIC_SITE_URL` ใน `.env.example` พร้อมคำอธิบาย | — (ต้องตั้งค่าจริงก่อน deploy) |
| 18 | คลาสขนาดฟอนต์ตีกัน (`text-body-md` ชน `text-sm`) 3 จุด | เหลือคลาสเดียวต่อ element | — |
| 19 | footer variant `product` เป็น `md:grid-cols-4` แต่มีลูก 3 → คอลัมน์ที่ 4 ว่าง | เปลี่ยนเป็น `md:grid-cols-3` | วัดได้ 3 คอลัมน์ / ลูก 3 ตัว พอดี |
| 20 | `© 2026` hardcode 3 จุด | ใช้ `new Date().getFullYear()` (`SiteFooter` เป็น server component อยู่แล้ว) | เรนเดอร์ออกมาเป็น `© 2026 KORN & COINS. สงวนลิขสิทธิ์` |
| 25 | `will-change: transform` ค้างถาวรบน `.nav-gpu` กินหน่วยความจำเปล่า | เอาออก คง `translateZ(0)` ไว้แก้บั๊ก Safari ตามคอมเมนต์เดิม | `will-change: auto` และ `transform` ยังทำงาน |
| 27 | `force-dynamic` ทำให้ยิงคิวรีฐานข้อมูลใหม่ทุก request ทั้งที่ข้อมูลแทบไม่เปลี่ยน | หน้าแรกและหน้าสินค้าเปลี่ยนเป็น `revalidate = 300` + เพิ่ม `generateStaticParams` ให้หน้าสินค้า | build จาก 10 → **37 หน้า static** · หน้าแรกเป็น `○ Static (5m)` · หน้าสินค้าเป็น `● SSG` prerender ครบ 26 slug |

**เรื่อง #27 ที่ต้องรู้:**
- `revalidatePath()` ใน `/api/admin/products` **มีอยู่แล้ว** เพิ่มสินค้าใหม่จึงล้างแคชทันที ไม่ต้องรอ 5 นาที
- `/admin` ยังเป็น `force-dynamic` (ต้องสดเสมอเพราะมี auth + ข้อความลูกค้า)
- `/watches/rolex-submariner` อ่าน `searchParams` จึงเป็น dynamic โดยธรรมชาติ แก้ไม่ได้และไม่จำเป็น
- ถ้าต่อฐานข้อมูลไม่ได้ตอน build จะ fallback เป็นแคตตาล็อกในโค้ด build ไม่พัง
- ⚠️ ถ้าลบสินค้าออกจาก DB โดยตรง หน้าที่ prerender ไว้จะยังอยู่จนครบ 5 นาที (ยังไม่มี endpoint ลบสินค้า จึงยังไม่กระทบตอนนี้)

## สรุป: แก้แล้ว 28 / 28 ข้อ

Phase 1, 2, 4 ตรวจยืนยันด้วยการทดสอบจริงครบ · Phase 3 เขียนโค้ดเสร็จแต่ 5 ใน 6 ข้อยังต้องดูด้วยตาในหน้า admin

## สิ่งที่ต้องทำก่อนขึ้นเว็บจริง

1. **ตั้ง `DATABASE_URL`** แล้วรัน `npm run db:migrate` — ไม่งั้นฟอร์มลูกค้าจะรับข้อความไม่ได้ (จะขึ้นเบอร์ร้านให้แทน ไม่ได้พังเงียบ)
2. **รัน `npm run db:seed`** ถ้าใช้ฐานข้อมูล เพื่อให้สินค้า 26 รายการเข้า DB
3. **ตั้ง `NEXT_PUBLIC_SITE_URL`** (ข้อ #13) ก่อนแชร์ลิงก์ที่ไหน
4. **เข้า `/admin` แล้วไล่ดู Phase 3 ทั้ง 6 ข้อด้วยตา** โดยเฉพาะการเพิ่มสินค้าใหม่และการอัปโหลดรูป
5. merge `redesign/luxury-dark-gold` → `main`

## หมายเหตุการทดสอบ

- ตอนทดสอบใช้ Postgres ชั่วคราวใน Docker (`mong-inquiry-test` พอร์ต 55432) **ลบทิ้งแล้ว** ไม่ได้แตะฐานข้อมูลของโปรเจกต์อื่นบนเครื่อง
- ระหว่างทางเจอ dev server ขึ้น 500 `__webpack_modules__ is not a function` สองครั้ง — เป็นอาการ HMR cache ค้างหลังเพิ่มไฟล์ใหม่ ไม่ใช่บั๊กโค้ด (`npm run build` ผ่านตลอด) แก้ด้วยการรีสตาร์ท dev server
